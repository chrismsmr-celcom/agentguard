# 🐕🦺 CerbereAG MCP Server

**Runtime Security for AI Agents — Now available as an MCP Server**

CerbereAG MCP allows AI agents (Claude, Cursor, Cline, custom agents) to self-audit their prompts, tool calls, and data flows in real-time using the Model Context Protocol.

## 🚀 Quick Start (30 seconds)

### Option 1: Install via pip
```bash
pip install cerbereag-mcp
