# =============================================================================
# AgentGuard - ML Dependencies (Optionnel)
# =============================================================================
# Installe avec : pip install -r requirements-ml.txt
# Utilisé uniquement si AGENTGUARD_USE_ML=true
# =============================================================================

# Inclus les dépendances de base
-r requirements.txt

# ML Stack
torch==2.1.2
transformers==4.36.2
datasets==2.16.1
scikit-learn==1.3.2
huggingface-hub==0.20.2    # Pour download des modèles au build Docker
accelerate==0.25.0         # Optimisation inference
sentencepiece==0.1.99      # Tokenizer pour certains modèles
protobuf==4.25.1           # Requis par transformers