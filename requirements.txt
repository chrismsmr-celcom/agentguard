# =============================================================================
# AgentGuard - Production Dependencies
# =============================================================================
# Installe avec : pip install --no-cache-dir -r requirements.txt
# Pour ML       : pip install -r requirements-ml.txt (séparé pour réduire l'image Docker)
# =============================================================================

# -----------------------------------------------------------------------------
# CORE - Framework Web & API
# -----------------------------------------------------------------------------
flask==3.0.0
werkzeug==3.0.1            # generate_password_hash, safe_str_cmp
itsdangerous==2.1.2        # Signature des cookies/session
flask-cors==4.0.0          # CORS pour le dashboard
gunicorn==21.2.0           # WSGI server (prod only)
python-dotenv==1.0.0       # Chargement propre de .env

# -----------------------------------------------------------------------------
# NETWORKING & HTTP
# -----------------------------------------------------------------------------
requests==2.31.0
urllib3==2.1.0             # Fix CVE-2023-45803
certifi==2023.11.17        # CA bundle à jour

# -----------------------------------------------------------------------------
# SECURITY - Rate Limiting & Auth
# -----------------------------------------------------------------------------
flask-limiter==3.5.0
redis==5.0.1               # Backend pour rate-limit distribué + cache LLM Judge
cryptography==41.0.7       # Chiffrement (si besoin)

# -----------------------------------------------------------------------------
# SECURITY - Détection (Budget + PII)
# -----------------------------------------------------------------------------
tiktoken==0.5.2            # Tokenisation précise pour le calcul de coût
presidio-analyzer==2.2.355 # Détection PII (standard Microsoft)
presidio-anonymizer==2.2.355

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
psycopg2-binary==2.9.9     # PostgreSQL (pour dev/simple deploy)
# psycopg[binary]==3.1.18  # Alternative moderne (décommente si migration vers psycopg3)

# -----------------------------------------------------------------------------
# VALIDATION & SERIALIZATION
# -----------------------------------------------------------------------------
pydantic>=2.7.0,<3.0.0            # Validation stricte des payloads API
pydantic-settings>=2.2.0,<3.0.0   # Configuration via variables d'env

# -----------------------------------------------------------------------------
# OBSERVABILITY - Logging & Tracing (OpenTelemetry)
# -----------------------------------------------------------------------------
structlog==23.2.0          # Logs JSON structurés
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-exporter-otlp-proto-http==1.22.0
opentelemetry-instrumentation-flask==0.43b0
opentelemetry-instrumentation-requests==0.43b0

# -----------------------------------------------------------------------------
# ML - Optionnel (dédié à requirements-ml.txt pour Docker léger)
# -----------------------------------------------------------------------------
# Voir requirements-ml.txt pour torch + transformers
# Installés via : pip install -r requirements-ml.txt
